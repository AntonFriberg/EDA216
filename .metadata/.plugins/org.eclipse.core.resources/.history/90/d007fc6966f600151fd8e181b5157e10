import java.awt.BorderLayout;
import java.awt.Dimension;
import java.awt.GridLayout;

import javax.swing.DefaultListModel;
import javax.swing.JFrame;
import javax.swing.JPanel;
import javax.swing.JScrollPane;
import javax.swing.JTable;
import javax.swing.JTextField;

public class PalletDetailGUI extends JFrame {
	private int rows = 5;
	private int cols = 5;

	private JTextField[][] fields = new JTextField[rows][cols];

	/**
	 * 
	 */
	private static final long serialVersionUID = 1L;

	public PalletDetailGUI(DefaultListModel<Pallet> palletListModel) {
		JPanel panel=new JPanel();
		panel.setLayout(new BorderLayout());
		
		String[] colNames = { "Pallet Id", "Cookie name", "Location", "Bake date", "Blocked", "Bill Id" };
		String[][] palletData = new String[palletListModel.size()][6];

		for (int i = 0; i < palletListModel.size(); i++) {
			palletData[i][0] = Integer.toString(palletListModel.getElementAt(i).getPalletNbr());
			palletData[i][1] = palletListModel.getElementAt(i).getCookieName();
			palletData[i][2] = palletListModel.getElementAt(i).getLocation();
			palletData[i][3] = palletListModel.getElementAt(i).getBakeDate();
			palletData[i][4] = palletListModel.getElementAt(i).isBlocked() != null ? "Y" : "N";
			palletData[i][5] = "-";
			// palletData[i][5]=Integer.toString(palletListModel.getElementAt(i).getBill().getBillId());
		}
		JTable table = new JTable(palletData, colNames);
		table.setPreferredScrollableViewportSize(new Dimension(500, 70));
		table.setFillsViewportHeight(true);
		
		JScrollPane pane=new JScrollPane(table);
		panel.add(pane);

		/*
		 * setLayout(new GridLayout(rows,cols)); setPreferredSize(new
		 * Dimension(350,200)); fields[0][0]=new JTextField("Något:");
		 * fields[1][0]=new JTextField("Något:"); fields[2][0]=new
		 * JTextField("Något:"); fields[3][0]=new JTextField("Något:");
		 * fields[4][0]=new JTextField("Något:	");
		 * 
		 * for(int i=0;i<rows;i++){ fields[i][0].setEditable(false);
		 * add(fields[i][0]); add(new JTextField()); }
		 */

		pack();
		setVisible(true);
	}
}
